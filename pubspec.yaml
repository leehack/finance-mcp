name: finance_mcp
description: A MCP server to fetch financial statements from SEC EDGAR.
repository: https://github.com/leehack/finance-mcp
version: 1.0.0
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.7.0
  html: ^0.15.5
  http: ^1.6.0
  logging: ^1.3.0
  mcp_dart: ^1.1.1


executables:
  finance_mcp: server

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
